<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://up-de-fra1-mysql-1.db.run-on-erla.com:11550/db-em8drk9p60sq</property>
        <property name="connection.username">db-em8drk9p60sq</property>
        <property name="connection.password">ggHveHwgaLort00gYreoW60h</property>

        <!-- JDBC connection pool settings -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQL5Dialect</property>

        <!-- Enable Hibernate's automatic session context management -->
        <property name="current_session_context_class">thread</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Mapping files or Annotated classes -->
        <!-- <mapping resource="com/example/YourEntity.hbm.xml"/> -->
        <!-- <mapping class="com.example.YourEntity"/> -->
    </session-factory>
</hibernate-configuration>
